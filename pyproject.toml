[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "spectroelectrochem-suite"
version = "6.0.0"
description = "Open-source analysis software for Raman, SERS, spectroelectrochemistry, RRDE, ECL, diffusion, EIS, stripping voltammetry and surface-activation/SERS data."
authors = [{ name = "Achim Habekost" }]
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
  "numpy",
  "pandas",
  "matplotlib",
  "scipy",
  "pybaselines",
  "openpyxl",
  "plotly",
  "PySide6",
  "xlsxwriter"
]

[project.scripts]
spectroelectrochem-suite = "spectroelectrochem_suite.modern_app:main"

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
spectroelectrochem_suite = ["plugins/*.json"]
